# this module takes a parsing-tree and restores the state by stub syscalls
from orderedset import OrderedSet
from ordereddict import OrderedDict
import sys

def stack_handler(stack=[], linker=OrderedDict()):
    return

stack = []

